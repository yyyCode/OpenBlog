# 推荐（宝塔 / 云服务器只放行 80、443）：留空，由 Nginx 把 /api 反代到本机 8082，浏览器不再直连 8082
# 若留空，请确认站点配置里已有 location /api/ { proxy_pass http://127.0.0.1:8082; ... }（见 deploy/nginx-frontend.example.conf）
VITE_API_BASE=

# 备选：浏览器直连后端 8082（必须在云安全组 / 防火墙放行 8082，否则接口永远连不上）
# VITE_API_BASE=http://8.138.32.217:8082
